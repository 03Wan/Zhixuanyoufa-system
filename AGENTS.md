## 一、项目基本信息

项目名称：智选优发——商品素材评估与发布决策辅助系统

项目类型：互联网+比赛展示版 MVP 系统

项目定位：

本项目是一个面向跨境电商企业的商品素材发布前智能评估与发布决策辅助系统。系统主要用于帮助跨境电商卖家、代运营机构、品牌出海企业在商品正式上架或广告投放前，对商品标题、卖点文案、详情页文案、广告语、商品图片等素材进行智能检测、风险评估、本土化优化和发布决策。

本阶段开发目标不是完整商业化系统，而是比赛展示版 MVP，重点保证核心业务闭环完整、页面展示效果专业、系统逻辑清晰、便于路演演示和后续扩展。

---

## 二、核心业务流程

系统 MVP 的核心流程为：

用户注册 / 登录  
→ 进入用户工作台  
→ 创建商品素材检测任务  
→ 填写商品信息与上传素材  
→ 后端规则引擎进行检测分析  
→ 输出素材质量评分  
→ 输出风险等级  
→ 输出本土化优化建议  
→ 输出发布决策  
→ 生成审核报告  
→ 在报告中心查看历史报告

核心闭环必须优先完成：

登录注册 → 创建检测任务 → 素材录入 → 智能检测 → 风险评估 → 优化建议 → 发布决策 → 报告生成

---

## 三、技术栈规范

### 1. 前端技术栈

前端使用：

- Next.js
- React
- TypeScript
- Tailwind CSS
- shadcn/ui
- lucide-react 图标库
- recharts 或简单 Tailwind 图表用于数据展示

前端要求：

- 使用 TypeScript 编写；
- 使用组件化开发；
- 页面风格要现代、简洁、科技感、专业；
- 页面适合互联网+比赛路演展示；
- 桌面端优先，移动端简单适配；
- 所有页面必须能正常跳转；
- 所有主要按钮应有实际交互；
- 不要只做静态页面；
- API 请求统一封装在 `src/lib/api.ts` 或 `lib/api.ts` 中；
- 页面业务逻辑不要全部堆在组件中，应适当拆分 hooks、utils 和 components。

---

### 2. 后端技术栈

后端使用：

- NestJS
- TypeScript
- Prisma ORM
- MySQL
- JWT 鉴权
- RESTful API

后端要求：

- 使用模块化结构；
- Controller 只负责接口入口；
- Service 负责业务逻辑；
- DTO 负责参数校验；
- Prisma 负责数据库访问；
- 检测逻辑放在 detection 模块；
- 不要把检测逻辑写死在 Controller 中；
- 所有接口返回统一格式；
- 代码结构要便于后续扩展真实 AI 模型、企业权限、套餐系统和 API 开放能力。

统一接口返回格式：

```ts
{
  code: 0,
  message: "success",
  data: {}
}
```
